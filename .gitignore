# 任务过程文档：需求 / 设计 / 任务拆分 / 状态 / 派发说明 / worker 报告
# 只在本机保留，不入仓
task/

# 项目本地 Claude 说明，只在本机（用户定）
CLAUDE.md

# 多 agent 协作的运行时状态（花名册 / 落库基线 / 报告）
.agents-orchestration/

.DS_Store

# 编辑器本地配置
.vscode/

# 两个通道仓的嵌套 clone（各自独立 git 仓，本仓不跟踪）
lark-connector/
ntfy-connector/
